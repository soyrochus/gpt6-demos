/** Desktop intentionally omits all photographic references and their paths. */
export function referenceImage(_index: number, _label: string) { return ''; }
export function aboutImage() { return ''; }
